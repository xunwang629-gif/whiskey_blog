BILIBILI_RSS_URL=https://your-rsshub-domain.example.com/bilibili/user/video/7858870
CRON_SECRET=replace-with-a-random-string
NOTION_TOKEN=secret_xxx
# Recommended: point directly to the community posts data source.
NOTION_DATA_SOURCE_ID=your-notion-data-source-id
# Optional fallback: if you only know the database ID, the app will try to auto-discover
# the data source when the database contains exactly one child source.
# NOTION_DATABASE_ID=your-notion-database-id
# Community pages also reuse the same Notion token/data source.
